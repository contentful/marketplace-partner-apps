{
  "name": "@contentful/marketplace-partner-apps",
  "private": false,
  "engines": {
    "node": ">=16.0.0 < 18.0.0",
    "npm": ">= 8.0.0 < 9.0.0"
  },
  "packageManager": "npm@8.19.4",
  "workspaces": [
    "apps/**"
  ],
  "devDependencies": {
    "contentful-cli": "^2.4.2",
    "husky": "^8.0.0",
    "lint-staged": "^13.2.1",
    "prettier": "2.8.7",
    "turbo": "^1.9.3"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "deploy:staging": "turbo run deploy:staging",
    "deploy": "turbo run deploy",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{t,j}s?(x)": [
      "prettier --write"
    ]
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
