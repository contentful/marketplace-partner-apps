{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "test": {
      "dependsOn": ["^test"],
      "inputs": []
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "deploy:staging": {
      "dependsOn": ["build", "test"]
    },
    "deploy": {
      "dependsOn": ["build", "test"]
    }
  }
}
