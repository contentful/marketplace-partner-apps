import { css } from '@emotion/css';

export const entryEditorStyles = {
  addVariationWrapper: css({
    marginTop: '0.75rem',
  }),
  formControl: css({
    borderLeftWidth: '3px',
    borderLeftColor: '#d1d9df',
    borderLeftStyle: 'solid',
    padding: '1rem',
  }),
  removeVariationButton: css({
    color: '#0059C8',
    bottom: '.25rem',
    position: 'relative',
    padding: 5,
  }),
};
