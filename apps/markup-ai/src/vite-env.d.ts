/// <reference types="vite/client" />

/** App version, populated from package.json at build time via Vite's `define`. */
declare const __APP_VERSION__: string;
/** npm package name, populated from package.json at build time via Vite's `define`. */
declare const __APP_PACKAGE_NAME__: string;
